#include "MergeSort.hpp"

MergeSort::MergeSort(const int &NUM_ELTS, int *currIdx, int *swaps, int *comparisons) : 
        NUM_ELEMENTS(NUM_ELTS), idx(currIdx), numSwaps(swaps), numComparisons(comparisons) {}

bool MergeSort::step(std::vector<float> &elements) {
    
    return false;
}